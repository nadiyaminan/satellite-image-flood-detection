services:
  - type: web
    name: flood-detection-ui
    env: static
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://flood-detection-api.onrender.com
